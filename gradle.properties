org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8 --add-exports jdk.compiler/com.sun.tools.javac.code=ALL-UNNAMED
org.gradle.caching=true
org.gradle.tooling.parallel=true

# Bump these for a release; keep them out of app/build.gradle so version-only
# changes do not invalidate the CI Gradle dependency cache key.
VERSION_CODE=557
VERSION_NAME=5.5.7
